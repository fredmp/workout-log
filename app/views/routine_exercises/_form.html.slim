= simple_form_for [@routine, @routine.routine_exercises.build], html: { class: 'form-horizontal' } do |f|
  = f.error_notification
  = f.collection_select :exercise_id, @exercises, :id, :name, { prompt: 'Choose an exercise' }, class: 'dropdown-toggle'
  = f.input :sets
  = f.input :reps
  = f.input :weight
  = f.input :duration
  = f.button :submit